# Copy to .streamlit/secrets.toml for local secrets. Never commit the real file.
# DATABASE_URL is normally supplied as a hosting-platform environment variable.
DATABASE_URL = "postgresql://USER:PASSWORD@HOST:5432/DATABASE"

